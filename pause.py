#!/usr/bin/python3

import time
def dot():
        dot.hello = "helo"
        TS =  time.sleep
        print("...", end="", flush=True)
        TS(0.1)
        print (".", end="", flush=True)
        TS(0.1)
        print (".", end="", flush=True)
        TS(0.1)
        print (".", end="", flush=True)

